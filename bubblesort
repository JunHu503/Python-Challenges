def bubblesort(x):
    temp =0
    for i in range (0,len(x)):
        for b in range (0,len(x)-1):
            if x[b]>x[b+1]:
                temp=x[b+1]
                x[b+1]=x[b]
                x[b]=temp
    print(x)
    return (x)
list=[1,2,4,2,64,24,6,84,2]
bubblesort(list)
